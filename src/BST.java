import javax.swing.tree.TreeNode;

/**
 * Created by brianyan on 10/25/15.
 */
public class BST {

    // Given a tree in inorder & post order, construct the binary tree
//    public TreeNode constructTree (int[] preorder, int [] postorder) {
//
//        TreeNode s = new TreeNode();
//        preStart = 0;
//        int preEnd = preorder.length - 1;
//        int postStart = 0;
//        int postEnd = postorder.length - 1;
//return TreeNode;
      //  return construct(preorder, preStart, preEnd, postStart, postStart, postEnd);
    }
//    public TreeNode (int [] preorder, int preStart, int preEnd, int [] postStart, int postStart, int postEnd){
//        TreeNode n = new TreeNode(5);
//        // Mark the Last Node of post order is the root
//        // Now find the root in the inorder list
//        // Left of root is the left subtree
//        // Right of root is the right subtree
//        // if the list is > 1 on each side, keep going?
//        // if it is one, add left and add right
//        return ;
//    }

